# Cloudflare Pages SPA fallback — serve index.html for client-side routes
# (e.g. /admin/*) so deep links and refreshes work on preview deploys.
# Ignored by GitHub Pages (production), which uses 404.html instead.
/*    /index.html   200
